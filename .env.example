DATABASE_URL="mongodb+srv://USER:PASS@cluster.mongodb.net/jahezmart?retryWrites=true&w=majority"
PORT=3000
JWT_SECRET=replace_with_a_strong_secret
NODE_ENV=development

# For initial testing you can seed an admin user using the register endpoint
# or add a hashed password here for a user entry in the DB.
